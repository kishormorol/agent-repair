# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.venv/
venv/
.env

# Project outputs
outputs/
data/

# LaTeX build artifacts
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
paper/*.synctex.gz
paper/*.fdb_latexmk
paper/*.fls
paper/missfont.log
paper/preview/

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/

# Claude Code
.claude/

# Run outputs. The AWS studies write ~2 GB per run: model archives, trial
# exports and per-batch copies of the HotpotQA dev set. The provenance the
# papers cite — frozen protocols, data audits, cost estimates, per-batch
# audits, pooled analyses — is force-added and tracked; the bulk is not.
output/
tmp/

# Local working memory: operational and billing detail, not for a public repo
MEMORY.md
