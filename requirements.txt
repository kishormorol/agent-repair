# Core LLM serving + logprob access
# vLLM >= 0.19.0 required for Qwen3.6 architecture support.
# transformers >= 4.57.0 required (architecture name changed in Qwen3.6).
vllm>=0.19.0
transformers>=4.57.0
tokenizers>=0.21.0
accelerate>=0.34.0
huggingface_hub>=0.27.0

# Data / numerics
numpy>=1.26
pyarrow>=14
pandas>=2.0
datasets>=2.20,<3
scipy>=1.11.0
scikit-learn>=1.3.0

# Config / IO
pyyaml>=6.0

# Progress / plotting
tqdm>=4.66.0
matplotlib>=3.8.0
seaborn>=0.13.0

# Stats (mcnemar, bootstrap live in scipy/statsmodels)
statsmodels>=0.14.0
